from django.apps import AppConfig


class MemoryContextConfig(AppConfig):
    name = "apps.memory_context"
    label = "memory_context"
    verbose_name = "Memory Context"
    default_auto_field = "django.db.models.BigAutoField"
